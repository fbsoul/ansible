---

 - hosts: db

   become: true

   tasks:

   - name: install mysql server on almalinux
  
     dnf: name=mysql state=present update_cache=yes

   - name: start the mysql server 
     
     service: name=mysqld state=started enabled=yes
